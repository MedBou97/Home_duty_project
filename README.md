# 🏠 HomeDuty — Database Lab Project

**HomeDuty** is a family task management and planning application. It demonstrates a full-stack integration of a **PostgreSQL** database with a **Node.js** backend and a **React** frontend.

### 🛠️ Tech Stack & Features

- **Frontend:** React (Vite)
- **Backend:** Node.js / Express
- **Database:** PostgreSQL
- **SQL Concepts Implemented:**
  - Tables with PK/FK constraints
  - Sequences & Indexes
  - Views & Functions
  - Triggers
  - Complex Queries (`UNION`, `EXCEPT`, `HAVING`)
  - Cursor + Record loops

---

## 0) Prerequisites

Ensure you have the following installed before starting:

- **VS Code** (Recommended IDE)
- **Node.js** (v18+ recommended)
- **PostgreSQL** (Local installation) + **pgAdmin 4**
- **Git**

---

## 1) Get the Project Code

### Option A: Clone via Git

```bash
git clone <YOUR_REPO_URL>
cd <YOUR_REPO_FOLDER>
```
